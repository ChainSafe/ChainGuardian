export enum Level {
    INFO = "info",
    ERROR = "error",
}

export enum Horizontal {
    LEFT = "left",
    RIGHT = "right",
    CENTER = "center-horizontal",
}

export enum Vertical {
    TOP = "top",
    BOTTOM = "bottom",
    CENTER = "center-vertical",
}
